# Code Jump Tags 功能 ↔ 代码对照（给 AI 自己看的索引）

> 目的：用户用自然语言指一个「功能/组件」时，我（未来的 Claude）能立刻定位到对应代码，
> 不用每次从头摸。用户说的功能名往往很口语（比如「悬浮小字」「+号」「回收站展开」），
> 下面的「用户怎么说」列就是给我做模糊匹配用的。
>
> 本仓库是 microsoft/codetour 的 fork，被改造成「标签优先的代码跳转工具」。
> 维护本文件：**改了某功能就顺手更新这里的行**，保持可信。

> **怎么用本文件**：用户让我读完它之后，他在新对话里只要甩一个口语名词（「注释框」「文件夹」
> 「小字」「+号」「回收站」…），我应当能立刻答出那是什么、代码在哪、相关坑是什么。
> 下面 §0.5 术语速查就是为这个场景准备的——先扫它，再按需深入对应章节。

---

## 0.5 术语速查（用户甩一个词 → 我立刻对上）

| 用户可能说的词 | 指的是 | 代码落点 | 详见 |
|---|---|---|---|
| 注释框 / 编辑框 / 气泡 | 编辑已有标签的 CodeTour 式 comment 气泡（controller `codeJumpTags-edit`） | `src/lodestar/editThread.ts`（`openTagEditor`/`saveTagEdit`/`cancelTagEdit`/`deleteFromEditor`） | §4 |
| +号 / 加号 / 编辑模式 | 行号边的 `+`，靠内存 ambient tour 拉起 | `toggleEditMode`(`lodestar/commands.ts`) + `AMBIENT_TOUR_ID` | §4 |
| 加注释 / 打标签 | 在 `+` 框提交生成标签 | `addTag`(`recorder/commands.ts`) | §2/§4 |
| 文件夹 / 分组 | tree 里的 FolderNode；派生成 CodeTour 的 tour | `tree.ts` + `adapter.ts`；新建/改名/删=`lodestar/commands.ts` | §1/§2/§5 |
| 标签 / 条目 | TagNode（一个代码位置+注释），CodeTour 的 step | `types.ts`/`tree.ts`，节点 `player/tree/nodes.ts` | §1/§3 |
| 小字 / 上方小字 / 行尾小字 | 标记行的注释文字（CodeLens 或行尾内联） | `decorator.ts`（`TagCodeLensProvider` / `renderOptions.after`） | §3 |
| 悬浮 / hover | 停在标记行上弹完整注释 | `decorator.ts` 的 `hoverMessage` | §3 |
| 标记 / 图标 / gutter | 行号边的黄色图标 | `decorator.ts` 的 `TOUR_DECORATOR` | §3 |
| 树 / 侧边栏 / 列表 | 独立活动栏容器里的「标签」视图（含拖拽） | `player/tree/index.ts` + `nodes.ts` | §3 |
| 链接 / 复制链接 | 复制成 `vscode://` 深链 markdown | `copyTagLink`/`copyFolderLinks`(`lodestar/commands.ts`)；接收端 `extension.ts` handleUri | §2 |
| 跳转 / 点了不跳 | 点标签打开文件定位行 | `gotoLocation`(`lodestar/commands.ts`) + `resolveLine` | §1/§2 |
| 回收站 / 恢复 / 误删 | 删的东西进 `store.trash`，可恢复（文件夹**递归展开到子文件夹/单标签**） | `tree.ts`(`removeToTrash`/`restoreSelection`/`removeDescendantById`) + `restoreFromTrash` | §5 |
| 确认 / 不再询问 | 删除确认弹框 + 关闭开关 | `confirmDelete`(`lodestar/commands.ts`) + 配置 `codeJumpTags.confirmDelete` | §5 |
| 存盘 / 没保存 / 没刷新 | store.json 写入 + 派生缓存重算 | `persistence.ts`(`saveStore`→`rebuildTours`) | §1 |

---

## 0. 一句话心智模型（先读这个）

- **唯一真源 = `.code-jump-tags/store.json`**（工作区内）。结构是一棵 `tree`：文件夹(folder) + 标签(tag)，外加一个 `trash` 回收站数组。
- CodeTour 的 `store.tours`（mobx 可观察）被降级成**单向派生缓存**：每次存盘后由 `treeToTours()` 重算。UI 的装饰/CodeLens/树读的是这个派生缓存，**不要反向写它**。
- 概念映射：CodeTour 的 **tour ↔ Code Jump Tags 文件夹**，**step ↔ 标签**。散标签(根下不在任何文件夹)被聚合成一个合成 tour `(未分组)`，id 后缀 `__loose__`。
- tour.id 格式：`${workspaceId}::${folderId | __loose__}`。要从树节点拿真实 folderId，就 `tour.id.split("::")[1]`。
- **纯逻辑层 `src/lodestar/*`（除 persistence/commands/editThread 外）不 import vscode**，所以能用 vitest 跑单测（`test/lodestar/*.test.ts`）。

---

## 1. 数据与存储

| 关注点 | 代码 |
|---|---|
| 节点类型 TagNode / FolderNode / TreeNode / TrashedEntry / LodestarStore | `src/lodestar/types.ts` |
| 树增删改查、回收站、查重 | `src/lodestar/tree.ts`（`addTag` `createFolder`(含 parentId) `findNode` `removeNode` `moveNode` `isSelfOrDescendant`(拖动守卫) `removeDescendantById`(回收站拔子树节点) `findTagByLocation` `pushTrash` `removeToTrash` `restoreEntries` `restoreSelection`） |
| 读写 store.json、派生缓存重算 | `src/lodestar/persistence.ts`（`getStore` `loadStore` `saveStore` `rebuildTours`，cache 在模块级单例） |
| tree → CodeTour[] 派生（含 `(未分组)`） | `src/lodestar/adapter.ts`（`treeToTours` `tagToStep`；`LOOSE_TOUR_ID`/`LOOSE_TITLE`） |
| 行漂移兜底（line 对不上用 pattern 重定位） | `src/lodestar/relocate.ts`（`resolveLine`） |

**关键不变量**：`saveStore()` = 写文件 + `rebuildTours()`。任何改了 store 的命令最后都要 `await saveStore()`，否则 UI 不刷新。

**文件夹嵌套（0.2.0 起）**：数据层 `TreeNode` 本就递归，`findNode`/`moveNode`/`removeNode` 也递归，所以
`renameFolder`/`deleteFolder`/`copyFolderLinks`/拖拽/上下移动标签在嵌套下**自动生效**（都按 `findNode(folderId)`
定位）。渲染：`store.tours` 派生缓存只到顶层文件夹（它喂树根 + tour 选择器，子文件夹不能当顶层可播放
tour），子文件夹靠树视图 `CodeTourTreeProvider.getChildren` 在展开某文件夹时用 `findNode(folderId)` 回原始树取
子文件夹，逐个 `folderToTour()`（已导出）包成 `CodeTourNode` 递归渲染；该文件夹的直属标签仍走
`element.tour.steps`。子文件夹**只是分组**，不是可播放 tour。建文件夹用 `newSubfolder` → `createFolder(..., parentId)`。

**装饰必须看 `allTours` 而非 `tours`（0.3.0 修）**：`store.tours` 只含顶层文件夹，若装饰层读它，嵌套子文件夹里
的标签就**没有行内标记/CodeLens/hover**。所以新增 `treeToAllTours()`（递归收集所有深度的文件夹标签 + 根松散
标签）→ 存进 `store.allTours`（`rebuildTours` 里和 `tours` 一起重算），`decorator.ts` 的 `getTourSteps` 与 reaction
都改读 `store.allTours`。`tours`/`allTours` 分工：前者=树根/选择器（仅顶层），后者=装饰源（全量）。

**任意拖动文件夹（0.3.0）**：`handleDrag` 现在也把 `CodeTourNode`（文件夹，排除 `__loose__`）的 folderId 放进
拖拽数据；`handleDrop` 落到文件夹=进该文件夹，落到标签=成其兄弟，落到空白=回根。`moveNode` 对文件夹同样有效；
**守卫** `isSelfOrDescendant(store, draggedId, targetParentId)` 拒绝把文件夹拖进自己/自己的子孙（否则会把子树丢掉）。

---

## 2. 命令 ID → 处理函数（用户点的每个按钮/菜单）

| 命令 ID | 处理函数（文件） | 用户怎么说 |
|---|---|---|
| `codeJumpTags.goto` | `gotoLocation` (`lodestar/commands.ts`) | 「点标签跳转」「点了不跳/跳错行」 |
| `codeJumpTags.toggleEditMode` | `toggleEditMode` (`lodestar/commands.ts`) | 「+号开关」「进入注释/编辑模式」「行号边出现+」 |
| `codeJumpTags.exitEditMode` | 同上（同一个 toggle） | 「退出编辑模式」「关掉那个+」 |
| `codeJumpTags.addTag` | `addTag` (`recorder/commands.ts`) | 「加注释/加标签」「点+输入文字提交」 |
| `codeJumpTags.editNote` | `editNote`→`openTagEditor` (`editThread.ts`) | 「点小字改注释」「点上方那行字编辑」 |
| `codeJumpTags.saveTagEdit` | `saveTagEdit` (`editThread.ts`) | 编辑框「✓ 保存注释」 |
| `codeJumpTags.cancelTagEdit` | `cancelTagEdit` (`editThread.ts`) | 编辑框「✗ 取消」 |
| `codeJumpTags.deleteFromEditor` | `deleteFromEditor` (`editThread.ts`) | 编辑框标题栏「🗑 删除这条注释」 |
| `codeJumpTags.copyTagLink` | `copyTagLink` (`lodestar/commands.ts`) | 「复制为链接」（单条） |
| `codeJumpTags.copyFolderLinks` | `copyFolderLinks` (`lodestar/commands.ts`) | 「复制文件夹里全部链接」 |
| `codeJumpTags.newFolder` | `newFolder` (`lodestar/commands.ts`) | 「新建文件夹/分组」（根层） |
| `codeJumpTags.newSubfolder` | `newSubfolder` (`lodestar/commands.ts`) | 「新建文件夹」（命令 id 仍叫 newSubfolder；在右键的文件夹下嵌套建，`createFolder(..., parentId)`，任意深度。显示名与根层「新建文件夹」对齐文件系统） |
| `codeJumpTags.renameFolder` | `renameFolder` (`lodestar/commands.ts`) | 「重命名文件夹」 |
| `codeJumpTags.renameTag` | `renameTag` (`lodestar/commands.ts`) | 「重命名标签」（树里右键 → 输入框改 note，不开注释栏；tag id 由 `nodes.ts` 的 `tagLink.id` 带出） |
| `codeJumpTags.deleteFolder` | `deleteFolder` (`lodestar/commands.ts`) | 「删除文件夹」 |
| `codeJumpTags.deleteTag` | `deleteTag`（tag 分支）(`recorder/commands.ts`) | 「删除标签」（树里右键/编辑框外） |
| `codeJumpTags.moveTagBack` / `Forward` | `moveStep` (`recorder/commands.ts`) | 「上移/下移标签」 |
| `codeJumpTags.restoreFromTrash` | `restoreFromTrash` (`lodestar/commands.ts`) | 「回收站/恢复删除的」 |
| `codeJumpTags.showMarkers` / `hideMarkers` | `setShowMarkers` (`player/commands.ts`) | 「显示/隐藏标记」 |
| `/goto` 深链(vscode://patrick1099.code-jump-tags/...) | `extension.ts` 的 `handleUri` 分支 | 「点别处的链接跳回来」 |

命令注册入口：Code Jump Tags 自有命令在 `registerLodestarCommands()`（`lodestar/commands.ts` 末尾）；
继承自 CodeTour 的录制类命令在 `registerRecorderCommands()`（`recorder/commands.ts`）。

---

## 3. 视图 / 显示

| 功能 | 代码 | 用户怎么说 |
|---|---|---|
| 独立活动栏入口（左侧竖排图标的 ⌖ JumpTag，点开标签视图） | `package.json` `contributes.viewsContainers.activitybar`(容器 id `codeJumpTags`)+ 图标 `images/activitybar.svg`(24×24 准星,`currentColor`)；视图 `codeJumpTags.tags` 挂在该容器下 | 「左边那个图标」「不要放资源管理器里」 |
| 侧边栏树（文件夹+标签，拖拽归类/排序） | `src/player/tree/index.ts`（`CodeTourTreeProvider` + `TreeDragAndDropController`，MIME `application/vnd.code.tree.codeJumpTags`） | 「左边那个树」「拖进文件夹」 |
| 树节点：标签 label=注释首行、路径在 tooltip、点击=goto | `src/player/tree/nodes.ts`（`CodeTourStepNode`；`tagLink` 挂在节点上给复制链接用；`step` 挂 tag id） | 「列表显示注释」「悬浮显示路径」 |
| 行号边 gutter 图标 + hover 完整注释 | `src/player/decorator.ts`（`TOUR_DECORATOR` + `updateDecorations` 里的 `hoverMessage`） | 「标记图标」「悬浮看完整注释」 |
| 注释小字：行上方 CodeLens / 行尾内联（开关） | `src/player/decorator.ts`（`TagCodeLensProvider`=above；`renderOptions.after`=end；`getNotePosition()` 读配置） | 「上方小字」「右边小字」「黑框」「占固定列挤代码」 |
| 装饰/CodeLens 自动刷新 | `src/player/decorator.ts` 的两个 mobx `reaction`（监听 `store.tours`/`showMarkers`）+ `onDidChangeCodeLenses` | 「加了/删了注释没刷新」 |
| 状态栏（录制态等） | `src/player/status.ts`（ambient tour 时抑制录制提示） | —— |

**坑备忘**：`store.showMarkers` 初值是 `false`（`src/store/index.ts`），`registerDecorators` 末尾设成配置值(默认 true)触发 reaction 首刷。改这块小心别让 reaction 不触发。

---

## 4. 编辑模式 & 注释框（两个 comment controller，别搞混）

- **ambient controller**（id `codeJumpTags`，label 仍叫 "CodeTour"）：在 `player/index.ts` 的 `startPlayer()` 里建。它的 `commentingRangeProvider` 在 `store.isRecording` 时返回整文件范围 → 行号边才有 `+`。
  - 「编辑模式开关」靠在内存里起一个空的 **ambient tour**（`AMBIENT_TOUR_ID`，在 `constants.ts`）来强制建出这个 controller，从而让 `+` 出现；关闭=`endCurrentCodeTour(false)` 拆掉。代码：`toggleEditMode`（`lodestar/commands.ts`）。
- **edit controller**（id `codeJumpTags-edit`）：在 `src/lodestar/editThread.ts` 里**单独**建，专门用来「编辑已有标签」。和编辑模式/录制无关，随时能用。
  - `openTagEditor(tagId, seedBody?)` 在标签所在行开一个 Editing 模式的注释气泡，预填原注释。
  - ✓ 保存=`saveTagEdit`（读 `comment.body` 写回 tag）、✗ 取消=`cancelTagEdit`、🗑 删除=`deleteFromEditor`（进回收站）。
  - 气泡里的按钮靠 `package.json` 的 `comments/comment/context`（✓✗）和 `comments/commentThread/title`（🗑）菜单，`when: commentController == codeJumpTags-edit`。

**「点+在已打标签的行不覆盖、而是基于原注释改」**：`addTag`（`recorder/commands.ts`）里先 `findTagByLocation`，命中就 `openTagEditor(existing.id, 原注释+新输入)`，不再盲写。

**坑备忘**：comment 回复回调里 `thread.dispose()` 必须 `setTimeout(...,0)` 延后，否则提交转圈卡死（见 `addTag`）。

---

## 5. 删除 / 确认 / 回收站

| 功能 | 代码 |
|---|---|
| 删除前确认 + 「不再询问」 | `confirmDelete()` (`lodestar/commands.ts`)，读/写配置 `codeJumpTags.confirmDelete` |
| 删除 → 进回收站（不真删） | `removeToTrash()` (`tree.ts`)；调用方：`deleteFolder`、`deleteTag` tag 分支、`deleteFromEditor` |
| 回收站列表 + 恢复（**递归展开子文件夹**，**勾选父子级联**，可恢复任意一层节点） | `restoreFromTrash()` (`lodestar/commands.ts`，用 `window.createQuickPick`+`onDidChangeSelection` 实现级联:勾文件夹→子树全勾、子全取消→文件夹取消;`folderRowsDeepestFirst` 自底向上重算父勾选,`updating`+`sameSet` 防回写死循环) + `restoreSelection()`/`removeDescendantById()` (`tree.ts`，把子树里任意节点拔到根) |
| 回收站上限 50、最近在前 | `pushTrash()` (`tree.ts`，`TRASH_LIMIT`) |

**坑备忘**：`QuickPickItem` 自带 `kind` 字段（分隔符用），自定义判别字段别叫 `kind`（我们用 `row`）。

---

## 6. 配置项（用户在 settings 里调的）

| 配置 key | 默认 | 作用 | 读取处 |
|---|---|---|---|
| `codeJumpTags.showMarkers` | true | 显不显 gutter 标记 | `decorator.ts` / `player/commands.ts` |
| `codeJumpTags.notePosition` | `above` | 注释小字位置 above(CodeLens)/end(行尾) | `decorator.ts` `getNotePosition()` |
| `codeJumpTags.confirmDelete` | true | 删除是否弹确认 | `lodestar/commands.ts` `confirmDelete()` |

**身份（2026-06 改名后）**：
- 显示名 **Code Jump Tags**，publisher `patrick1099`，扩展 id `patrick1099.code-jump-tags`（= `${publisher}.${package.name}`）。已发布到 VS Code 市场。
- 命令前缀 / 上下文键 / controller / 配置 section = `codeJumpTags`（`EXTENSION_NAME` 常量，`src/constants.ts`）。
- 存储文件夹 `.code-jump-tags/`（`STORE_DIRECTORY`）；旧 `.lodestar/` 数据由 `persistence.loadStore` 自动迁移一次。
- 深链 `vscode://patrick1099.code-jump-tags/goto?file=..&line=..&pattern=..`（`tagLinkMarkdown` 里 `EXTENSION_ID`，**注意是扩展 id 不是命令前缀**；该值运行时从 `context.extension.id` 读取，<1.74 回退到字面量,改 publisher 不会再漂）。
- 仍叫 lodestar 的只有：`src/lodestar/` 目录名、import 路径、TS 符号 `LodestarStore`/`registerLodestarCommands`（纯内部，无所谓）。

---

## 7. 构建 / 测试 / 安装（我每轮改完都要跑）

```bash
# 在 C:\Users\dell\Desktop\plugin-research\codetour
npm run build            # webpack 生产构建 → dist/extension-node.js + extension-web.js（不是 esbuild/compile）
npm run test:unit        # vitest，跑 test/lodestar/*（纯逻辑）
npx @vscode/vsce package --no-dependencies -o code-jump-tags.vsix
"C:\Users\dell\AppData\Local\Programs\Microsoft VS Code\bin\code.cmd" --install-extension <abs>\code-jump-tags.vsix --force
# 改名遗留：旧 id 是 hsheng.lodestar,改名后是 patrick1099.code-jump-tags(两个不同扩展),
# 装新的之前要卸旧的: code.cmd --uninstall-extension hsheng.lodestar
# 用户侧生效：Developer: Reload Window
```

- TS 类型：`@types/vscode` 钉 `~1.66.0`（`^1.66` 会拉到 1.120 把 CommentThread.range 变可选炸编译）。engines.vscode `^1.66.0`（拖拽 API 下限）。
- **GUI 行为我没法在本机实测**（注释框按钮、装饰、拖拽、悬浮）。这类只能让用户重载验证；改这类时在回复里点名「需要你验 X」。
- 单测只覆盖 `src/lodestar/` 纯逻辑（tree/relocate/adapter/trash）。带 vscode 的文件不在单测范围。

---

## 8. 给未来的我：用户报问题时的定位顺序

1. 先归类是哪块：**显示**(§3)、**编辑模式/注释框**(§4)、**删除/回收站**(§5)、**数据没存/没刷新**(§1 不变量)。
2. 口语词对照 §2 的「用户怎么说」列找命令 → 跳处理函数。
3. 纯逻辑能写单测就先加单测复现（cheap，且能跑）；GUI 行为只能改完让用户重载验。
4. 改完更新本文件对应行。
