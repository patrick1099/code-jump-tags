# 未分组→真文件夹 + 多选批量操作 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把侧边栏「未分组」从合成组变成可改名/可拖/可删的真实文件夹(带「收件箱」身份),并让多选拖动/复制/删除批量生效。

**Architecture:** `FolderNode` 加 `inbox?` 标记;纯函数 `getOrCreateInbox` / `migrateLooseTags` 管理唯一收件箱;改名/移出根级/删除触发「毕业」(清 flag);`adapter` 移除合成的 `__loose__` 组;② 通过新纯函数 `pruneCovered` / `collectTagsUnder` 让现有 `deleteTag`/`deleteFolder`/`copyFolderLinks` 消费 VS Code 多选数组。

**Tech Stack:** TypeScript, VS Code Extension API, vitest(单测), webpack(构建)。

## Global Constraints

- 仅在 `C:\Users\dell\Desktop\plugin-research\codetour` 内改动;不碰 `C:\Users\dell\Desktop\需求`。
- 提交信息结尾追加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 纯逻辑层(`src/lodestar/tree.ts`、`adapter.ts`、新建 `selection.ts`)**禁止 import `vscode`**(保持可测)。
- TDD:每个纯函数先写失败测试再实现。现有 44 个测试须保持全绿。
- vsix(`*.vsix`)已 gitignore,**不提交**。
- 命令名前缀 `codeJumpTags.`;收件箱标题常量 `INBOX_TITLE = "未分组"`。
- 收件箱语义:始终最多一个;改名/移出根级/删除即毕业;新标签落进收件箱,无则懒创建并置顶(`unshift`)。
- 单测命令 `npx vitest run`;构建 `npm run build`。Bash 工具每次会把 cwd 重置到别处,命令统一前缀 `cd /c/Users/dell/Desktop/plugin-research/codetour && ...`。

---

### Task 1: `inbox` 字段 + `getOrCreateInbox`

**Files:**
- Modify: `src/lodestar/types.ts:15-21`(给 `FolderNode` 加 `inbox?: boolean`)
- Modify: `src/lodestar/tree.ts`(新增 `INBOX_TITLE`、`newFolderId`、`getOrCreateInbox`)
- Test: `test/lodestar/inbox.test.ts`(新建)

**Interfaces:**
- Consumes: `LodestarStore`, `FolderNode`(types.ts);`findNode`(tree.ts)。
- Produces:
  - `export const INBOX_TITLE = "未分组";`
  - `export function newFolderId(): string;`(并发安全的具体 id 生成器,测试可自带)
  - `export function getOrCreateInbox(store: LodestarStore, idGen: () => string): FolderNode;`(返回根级 `inbox===true` 的文件夹;无则在根顶部 `unshift` 一个 `{type:"folder",id:idGen(),title:INBOX_TITLE,inbox:true,children:[]}` 并返回)

- [ ] **Step 1: 加 `inbox` 字段**

`src/lodestar/types.ts` 的 `FolderNode` 加一行:

```ts
export interface FolderNode {
  type: "folder";
  id: string;
  title: string;
  inbox?: boolean;         // 该文件夹是「新标签收件箱」;改名/移出根级/删除即毕业
  ref?: string | null;     // reserved
  children: TreeNode[];    // v1 UI: only TagNode; recursive type reserves nesting
}
```

- [ ] **Step 2: 写失败测试**

新建 `test/lodestar/inbox.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { createEmptyStore } from "../../src/lodestar/tree";
import { getOrCreateInbox, INBOX_TITLE } from "../../src/lodestar/tree";
import { FolderNode, TagNode } from "../../src/lodestar/types";

let seq = 0;
const idGen = () => `f${++seq}`;

function folder(id: string, inbox = false): FolderNode {
  return { type: "folder", id, title: "F" + id, inbox: inbox || undefined, children: [] };
}

describe("getOrCreateInbox", () => {
  it("creates an inbox folder at the top of the root when none exists", () => {
    seq = 0;
    const s = createEmptyStore();
    s.tree.push(folder("a"));
    const inbox = getOrCreateInbox(s, idGen);
    expect(inbox.inbox).toBe(true);
    expect(inbox.title).toBe(INBOX_TITLE);
    expect(s.tree[0]).toBe(inbox);          // placed at index 0
    expect(s.tree).toHaveLength(2);
  });

  it("reuses the existing inbox and never creates a second one", () => {
    seq = 0;
    const s = createEmptyStore();
    s.tree.push(folder("a", true));
    const first = getOrCreateInbox(s, idGen);
    const second = getOrCreateInbox(s, idGen);
    expect(first).toBe(second);
    expect(s.tree.filter(n => n.type === "folder" && (n as FolderNode).inbox)).toHaveLength(1);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: FAIL —「getOrCreateInbox is not a function」/ 找不到导出。

- [ ] **Step 4: 实现**

在 `src/lodestar/tree.ts` 顶部 import 区下方加入:

```ts
export const INBOX_TITLE = "未分组";

export function newFolderId(): string {
  return `f_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`;
}

// The single new-tag inbox: the root-level folder flagged `inbox`. Returns it,
// or lazily creates one at the TOP of the root (matching where the old
// synthetic "(未分组)" group used to sit). At most one inbox ever exists.
export function getOrCreateInbox(
  store: LodestarStore,
  idGen: () => string
): FolderNode {
  const existing = store.tree.find(
    (n): n is FolderNode => n.type === "folder" && n.inbox === true
  );
  if (existing) return existing;
  const inbox: FolderNode = {
    type: "folder",
    id: idGen(),
    title: INBOX_TITLE,
    inbox: true,
    children: []
  };
  store.tree.unshift(inbox);
  return inbox;
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: PASS（2 个用例）。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/lodestar/types.ts src/lodestar/tree.ts test/lodestar/inbox.test.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): add inbox FolderNode flag + getOrCreateInbox

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `migrateLooseTags`(旧 store 迁移)

**Files:**
- Modify: `src/lodestar/tree.ts`(新增 `migrateLooseTags`)
- Test: `test/lodestar/inbox.test.ts`(追加 describe)

**Interfaces:**
- Consumes: `getOrCreateInbox`(Task 1)。
- Produces: `export function migrateLooseTags(store: LodestarStore, idGen: () => string): boolean;`(把根级所有 `type==="tag"` 的散标签按原顺序搬进收件箱;无散标签则不动;返回是否发生了迁移)

- [ ] **Step 1: 写失败测试**

在 `test/lodestar/inbox.test.ts` 末尾追加:

```ts
import { migrateLooseTags } from "../../src/lodestar/tree";

function tag(id: string): TagNode {
  return { type: "tag", id, note: "n" + id, file: "a.c", line: 1, createdAt: "t" };
}

describe("migrateLooseTags", () => {
  it("moves root-level loose tags into a new top inbox, preserving order", () => {
    seq = 0;
    const s = createEmptyStore();
    s.tree.push(tag("t1"), folder("a"), tag("t2"));
    const changed = migrateLooseTags(s, idGen);
    expect(changed).toBe(true);
    expect(s.tree[0].type).toBe("folder");
    const inbox = s.tree[0] as FolderNode;
    expect(inbox.inbox).toBe(true);
    expect(inbox.children.map(c => c.id)).toEqual(["t1", "t2"]);
    // only the folder "a" remains beside the inbox at root
    expect(s.tree.filter(n => n.type === "tag")).toHaveLength(0);
    expect(s.tree.map(n => n.id)).toEqual([inbox.id, "a"]);
  });

  it("does nothing and returns false when there are no loose tags", () => {
    seq = 0;
    const s = createEmptyStore();
    s.tree.push(folder("a"));
    expect(migrateLooseTags(s, idGen)).toBe(false);
    expect(s.tree.map(n => n.id)).toEqual(["a"]);
  });

  it("is idempotent (second run is a no-op)", () => {
    seq = 0;
    const s = createEmptyStore();
    s.tree.push(tag("t1"));
    expect(migrateLooseTags(s, idGen)).toBe(true);
    expect(migrateLooseTags(s, idGen)).toBe(false);
    const inbox = s.tree[0] as FolderNode;
    expect(inbox.children.map(c => c.id)).toEqual(["t1"]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: FAIL —「migrateLooseTags is not a function」。

- [ ] **Step 3: 实现**

在 `src/lodestar/tree.ts` 的 `getOrCreateInbox` 下方加入:

```ts
// One-time, idempotent: wrap any root-level loose tags (from before the inbox
// model) into the inbox folder, preserving their order. Returns true if it moved
// anything. Safe to call on every load.
export function migrateLooseTags(
  store: LodestarStore,
  idGen: () => string
): boolean {
  const looseTags = store.tree.filter(
    (n): n is TagNode => n.type === "tag"
  );
  if (looseTags.length === 0) return false;
  store.tree = store.tree.filter(n => n.type !== "tag");
  const inbox = getOrCreateInbox(store, idGen);
  inbox.children.push(...looseTags);
  return true;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: PASS（共 5 个用例）。

- [ ] **Step 5: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/lodestar/tree.ts test/lodestar/inbox.test.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): migrateLooseTags wraps legacy root tags into inbox

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 毕业逻辑(`moveNode` 移出根级 + `renameFolderNode`)

**Files:**
- Modify: `src/lodestar/tree.ts`(`moveNode` 加毕业;新增 `renameFolderNode`)
- Test: `test/lodestar/inbox.test.ts`(追加 describe)

**Interfaces:**
- Consumes: `findNode`, `removeNode`, `childrenOf`(tree.ts 既有)。
- Produces:
  - `moveNode` 行为扩展:当被移动节点是 `inbox` 文件夹且 `toParentId !== null`(移进某文件夹/移出根级)时,清除其 `inbox` 标记。根级内重排(`toParentId === null`)保留标记。
  - `export function renameFolderNode(store: LodestarStore, id: string, title: string): boolean;`(改标题;若该文件夹是收件箱则清 `inbox`;找不到/非文件夹返回 false)

- [ ] **Step 1: 写失败测试**

追加到 `test/lodestar/inbox.test.ts`:

```ts
import { moveNode, renameFolderNode } from "../../src/lodestar/tree";

describe("inbox graduation", () => {
  it("moveNode clears the inbox flag when moved under another folder", () => {
    seq = 0;
    const s = createEmptyStore();
    const inbox = getOrCreateInbox(s, idGen); // at root, inbox:true
    s.tree.push(folder("host"));
    moveNode(s, inbox.id, "host", 0);
    expect(inbox.inbox).toBeFalsy();          // graduated
  });

  it("moveNode keeps the inbox flag when reordered within the root", () => {
    seq = 0;
    const s = createEmptyStore();
    const inbox = getOrCreateInbox(s, idGen);
    s.tree.push(folder("a"));
    moveNode(s, inbox.id, null, 1);           // still at root
    expect(inbox.inbox).toBe(true);
  });

  it("renameFolderNode renames and graduates the inbox", () => {
    seq = 0;
    const s = createEmptyStore();
    const inbox = getOrCreateInbox(s, idGen);
    expect(renameFolderNode(s, inbox.id, "链路A")).toBe(true);
    expect(inbox.title).toBe("链路A");
    expect(inbox.inbox).toBeFalsy();
  });

  it("renameFolderNode returns false for a missing id", () => {
    seq = 0;
    const s = createEmptyStore();
    expect(renameFolderNode(s, "nope", "x")).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: FAIL —「renameFolderNode is not a function」及 moveNode 毕业断言失败。

- [ ] **Step 3: 实现**

`src/lodestar/tree.ts` 的 `moveNode` 改为(在 `splice` 前插入毕业判断):

```ts
export function moveNode(
  store: LodestarStore,
  id: string,
  toParentId: string | null,
  index: number
): void {
  const node = removeNode(store, id);
  if (!node) return;
  // An inbox folder graduates (loses its inbox role) the moment it leaves the
  // root for another folder. Reordering within the root keeps it the inbox.
  if (node.type === "folder" && node.inbox && toParentId !== null) {
    delete node.inbox;
  }
  const target = toParentId ? childrenOf(store, toParentId) : store.tree;
  const clamped = Math.max(0, Math.min(index, target.length));
  target.splice(clamped, 0, node);
}
```

在 `moveNode` 下方新增:

```ts
// Rename a folder. Renaming an inbox graduates it to a plain folder (its tags
// stay; the next new tag lazily spawns a fresh inbox). Returns false if `id`
// isn't a folder.
export function renameFolderNode(
  store: LodestarStore,
  id: string,
  title: string
): boolean {
  const found = findNode(store, id);
  if (!found || found.node.type !== "folder") return false;
  found.node.title = title;
  if (found.node.inbox) delete found.node.inbox;
  return true;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/inbox.test.ts`
Expected: PASS（共 9 个用例）。

- [ ] **Step 5: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/lodestar/tree.ts test/lodestar/inbox.test.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): inbox graduates on move-out-of-root and rename

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: adapter 移除合成的 `__loose__` 组

**Files:**
- Modify: `src/lodestar/adapter.ts:35-82`(`treeToTours` / `treeToAllTours` 去掉 loose 块)
- Test: `test/lodestar/adapter.test.ts`(追加用例)

**Interfaces:**
- Consumes: `folderToTour`(adapter.ts 既有)。
- Produces: `treeToTours` / `treeToAllTours` 不再产出任何 `__loose__` / 标题为 `(未分组)` 的合成 tour;根级散标签被忽略(迁移已负责包装)。`LOOSE_TOUR_ID` 常量保留导出(`tree/index.ts` 仍可能引用字符串,但本任务起不再用于合成)。

- [ ] **Step 1: 写失败测试**

在 `test/lodestar/adapter.test.ts` 末尾追加:

```ts
import { treeToTours, treeToAllTours } from "../../src/lodestar/adapter";
import { createEmptyStore } from "../../src/lodestar/tree";

describe("no synthetic loose group", () => {
  it("treeToTours ignores root-level loose tags (emits only folder tours)", () => {
    const s = createEmptyStore();
    s.tree.push({ type: "tag", id: "t1", note: "n", file: "a.c", line: 1, createdAt: "t" });
    s.tree.push({ type: "folder", id: "f1", title: "通信", children: [] });
    const tours = treeToTours(s, "ws");
    expect(tours).toHaveLength(1);
    expect(tours[0].id).toBe("ws::f1");
    expect(tours.some(t => t.id.endsWith("__loose__"))).toBe(false);
    expect(tours.some(t => t.title === "(未分组)")).toBe(false);
  });

  it("treeToAllTours emits no loose group", () => {
    const s = createEmptyStore();
    s.tree.push({ type: "tag", id: "t1", note: "n", file: "a.c", line: 1, createdAt: "t" });
    const all = treeToAllTours(s, "ws");
    expect(all.some(t => t.id.endsWith("__loose__"))).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/adapter.test.ts`
Expected: FAIL — 当前实现会产出 `ws::__loose__` 的「(未分组)」tour,断言不通过。

- [ ] **Step 3: 实现**

`src/lodestar/adapter.ts` 的 `treeToTours` 改为:

```ts
export function treeToTours(store: LodestarStore, workspaceId: string): CodeTour[] {
  const folders = store.tree.filter((n): n is FolderNode => n.type === "folder");
  return folders.map(f => folderToTour(f, workspaceId));
}
```

`treeToAllTours` 删去开头的 loose 块,改为:

```ts
export function treeToAllTours(
  store: LodestarStore,
  workspaceId: string
): CodeTour[] {
  const tours: CodeTour[] = [];

  function walk(nodes: (FolderNode | TagNode)[]): void {
    for (const node of nodes) {
      if (node.type === "folder") {
        tours.push(folderToTour(node, workspaceId));
        walk(node.children as (FolderNode | TagNode)[]);
      }
    }
  }
  walk(store.tree as (FolderNode | TagNode)[]);

  return tours;
}
```

（`TagNode` 已在文件内通过 `./types` 引入;若 lint 报 `TagNode` 未用,保留它用于 `walk` 的类型注解即可。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/adapter.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/lodestar/adapter.ts test/lodestar/adapter.test.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): drop synthetic (未分组) group from adapter

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 接线 — 新标签落收件箱 + 加载时迁移

**Files:**
- Modify: `src/recorder/commands.ts:433`(`addTag(getStore(), tag)` → 落收件箱)
- Modify: `src/recorder/commands.ts:22`(import 增加 `getOrCreateInbox`, `newFolderId`)
- Modify: `src/lodestar/persistence.ts:31-50`(`loadStore` 加迁移)
- Test: 无新单测(VS Code 胶水);靠 Task 1/2 纯测试覆盖 + 构建/回归。

**Interfaces:**
- Consumes: `getOrCreateInbox`, `newFolderId`, `migrateLooseTags`(tree.ts)。
- Produces: 新标签进入收件箱;打开旧 store 时根级散标签被迁移。

- [ ] **Step 1: 新标签落收件箱**

`src/recorder/commands.ts` 顶部 import(原第 22 行附近 `import { addTag, findTagByLocation } from "../lodestar/tree";`)改为:

```ts
import {
  addTag,
  findTagByLocation,
  getOrCreateInbox,
  newFolderId
} from "../lodestar/tree";
```

把第 433 行 `addTag(getStore(), tag); // loose (root) by default` 改为:

```ts
const inbox = getOrCreateInbox(getStore(), newFolderId);
addTag(getStore(), tag, inbox.id); // new tags land in the inbox folder
```

- [ ] **Step 2: 加载时迁移**

`src/lodestar/persistence.ts` 顶部 import(第 6 行）改为:

```ts
import { createEmptyStore, migrateLooseTags, newFolderId, parse, serialize } from "./tree";
```

在 `loadStore` 的 `rebuildTours();`(第 49 行)之前插入一行:

```ts
  migrateLooseTags(cache, newFolderId);
  rebuildTours();
```

- [ ] **Step 3: 构建 + 全量回归**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npm run build && npx vitest run`
Expected: webpack 零错误;vitest 全绿（含 Task 1-4 新增）。

- [ ] **Step 4: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/recorder/commands.ts src/lodestar/persistence.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): route new tags to inbox + migrate loose tags on load

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 解除拦截 + 拖放接线(`tree/index.ts` 与 `lodestar/commands.ts`)

**Files:**
- Modify: `src/player/tree/index.ts:34-87`(`handleDrag`/`handleDrop` 去 `__loose__` 特判;标签拖空白落收件箱)
- Modify: `src/player/tree/index.ts:179-224`(`subfolderNodesOf`/`getParent` 去 `__loose__` 特判)
- Modify: `src/lodestar/commands.ts:203-223`(`renameFolder` 去拦截、调用 `renameFolderNode`)
- Modify: `src/lodestar/commands.ts:266-296`(`deleteFolder` 去拦截)
- Modify: `src/lodestar/commands.ts:451-472`(`newSubfolder` 去拦截)
- Test: 无新单测;靠纯测试 + 构建 + 手测。

**Interfaces:**
- Consumes: `renameFolderNode`, `getOrCreateInbox`, `newFolderId`, `moveNode`, `findNode`, `isSelfOrDescendant`(tree.ts)。
- Produces: 收件箱与普通文件夹一样可改名/删/建子文件夹;标签拖到空白处落入收件箱(并触发 `moveNode` 的毕业规则,文件夹除外)。

- [ ] **Step 1: `handleDrag` 去 `__loose__`**

`src/player/tree/index.ts` 的 `handleDrag` 内,把折叠 id 的那段(第 44-48 行)简化为(folder 一律可拖):

```ts
        if (s instanceof CodeTourNode) {
          const folderId = s.tour.id.split("::").pop();
          return folderId || undefined;
        }
```

- [ ] **Step 2: `handleDrop` — 去特判 + 标签拖空白落收件箱**

`handleDrop` 内,动态 import 增加 `getOrCreateInbox` / `newFolderId`,并把目标解析与循环改为:

```ts
    const { getStore, saveStore } = await import("../../lodestar/persistence");
    const { moveNode, findNode, isSelfOrDescendant, getOrCreateInbox, newFolderId } =
      await import("../../lodestar/tree");
    const store = getStore();

    let toParentId: string | null = null;
    let index = Number.MAX_SAFE_INTEGER;
    if (target instanceof CodeTourNode) {
      toParentId = target.tour.id.split("::").pop()!;
    } else if (target instanceof CodeTourStepNode && target.step?.id) {
      const found = findNode(store, target.step.id);
      if (found) {
        toParentId = found.parent ? found.parent.id : null;
        index = found.index;
      }
    }
    for (const id of ids) {
      let dest = toParentId;
      // Dropping a TAG on empty space lands it in the inbox (there are no
      // root-level loose tags anymore). A folder dropped on empty space stays
      // at the root.
      if (dest === null && target === undefined) {
        const node = findNode(store, id);
        if (node && node.node.type === "tag") {
          dest = getOrCreateInbox(store, newFolderId).id;
        }
      }
      if (dest && isSelfOrDescendant(store, id, dest)) continue;
      moveNode(store, id, dest, index);
    }
    await saveStore();
```

- [ ] **Step 3: `subfolderNodesOf` / `getParent` 去 `__loose__`**

`subfolderNodesOf`(第 182-184 行）把:

```ts
    const folderId = node.tour.id.split("::").pop();
    if (!folderId || folderId === "__loose__") return [];
```

改为:

```ts
    const folderId = node.tour.id.split("::").pop();
    if (!folderId) return [];
```

`getParent`(第 207-208 行）把 `if (folderId && folderId !== "__loose__") {` 改为 `if (folderId) {`。

- [ ] **Step 4: `lodestar/commands.ts` 解除三处拦截**

`renameFolder`(第 204-222 行）改为(去掉 `LOOSE_TOUR_ID` 分支,改走纯函数):

```ts
export async function renameFolder(node: any) {
  const tourId: string | undefined = node?.tour?.id;
  const folderId = tourId ? tourId.split("::")[1] : undefined;
  const store = getStore();
  const found = folderId ? findNode(store, folderId) : undefined;
  if (!found || found.node.type !== "folder") {
    window.showInformationMessage("Code Jump Tags: 找不到该文件夹");
    return;
  }
  const title = await window.showInputBox({
    prompt: "文件夹名称",
    value: found.node.title
  });
  if (title === undefined || title.trim() === "") return;
  renameFolderNode(store, folderId!, title.trim());
  await saveStore();
}
```

并在文件顶部 `tree` 的 import(第 29-34 行）加入 `renameFolderNode`:

```ts
import {
  createFolder,
  findNode,
  removeToTrash,
  renameFolderNode,
  restoreSelection
} from "./tree";
```

`deleteFolder`(第 266-274 行）删掉 `LOOSE_TOUR_ID` 拦截块,改为:

```ts
export async function deleteFolder(node: any) {
  const tourId: string | undefined = node?.tour?.id;
  const folderId = tourId ? tourId.split("::")[1] : undefined;
  const store = getStore();
  const found = folderId ? findNode(store, folderId) : undefined;
  if (!found || found.node.type !== "folder") {
    window.showInformationMessage("Code Jump Tags: 找不到该文件夹");
    return;
  }
  // ...（下方 count/confirm/removeToTrash 保持不变）
```

`newSubfolder`(第 454-462 行）删掉 `LOOSE_TOUR_ID` 拦截块,改为:

```ts
export async function newSubfolder(node: any) {
  const tourId: string | undefined = node?.tour?.id;
  const folderId = tourId ? tourId.split("::").pop() : undefined;
  const found = folderId ? findNode(getStore(), folderId) : undefined;
  if (!found || found.node.type !== "folder") {
    window.showInformationMessage("Code Jump Tags: 找不到该文件夹");
    return;
  }
  // ...（下方 showInputBox/createFolder 保持不变）
```

若 `LOOSE_TOUR_ID` 在 `commands.ts` 中已无引用,删除第 18 行 `import { LOOSE_TOUR_ID } from "./adapter";`。

- [ ] **Step 5: 构建 + 回归**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npm run build && npx vitest run`
Expected: 零错误;vitest 全绿。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/player/tree/index.ts src/lodestar/commands.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): make inbox a real folder (rename/move/delete/subfolder)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: ② 批量纯函数(`selection.ts`)

**Files:**
- Create: `src/lodestar/selection.ts`
- Test: `test/lodestar/selection.test.ts`(新建)

**Interfaces:**
- Consumes: `findNode`(tree.ts);`LodestarStore`, `TagNode`, `TreeNode`(types.ts)。
- Produces:
  - `export function pruneCovered(store: LodestarStore, ids: string[]): string[];`(去掉「祖先也在选中集合里」的 id)
  - `export function collectTagsUnder(store: LodestarStore, ids: string[]): TagNode[];`(把选中项展开成树序、去重的标签列表;文件夹贡献其全部嵌套标签)

- [ ] **Step 1: 写失败测试**

新建 `test/lodestar/selection.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { createEmptyStore, addTag, createFolder } from "../../src/lodestar/tree";
import { pruneCovered, collectTagsUnder } from "../../src/lodestar/selection";
import { TagNode } from "../../src/lodestar/types";

function tag(id: string): TagNode {
  return { type: "tag", id, note: "n" + id, file: "a.c", line: 1, createdAt: "t" };
}

describe("pruneCovered", () => {
  it("drops a child whose ancestor folder is also selected", () => {
    const s = createEmptyStore();
    createFolder(s, "F", () => "f1");
    addTag(s, tag("t1"), "f1");
    expect(pruneCovered(s, ["f1", "t1"])).toEqual(["f1"]);
  });

  it("keeps unrelated nodes", () => {
    const s = createEmptyStore();
    addTag(s, tag("t1"));
    addTag(s, tag("t2"));
    expect(pruneCovered(s, ["t1", "t2"]).sort()).toEqual(["t1", "t2"]);
  });
});

describe("collectTagsUnder", () => {
  it("expands folders to their nested tags in tree order", () => {
    const s = createEmptyStore();
    createFolder(s, "F", () => "f1");
    addTag(s, tag("a"), "f1");
    addTag(s, tag("b"), "f1");
    addTag(s, tag("c")); // root-level
    const tags = collectTagsUnder(s, ["f1", "c"]);
    expect(tags.map(t => t.id)).toEqual(["a", "b", "c"]);
  });

  it("dedups a tag selected both directly and via its folder", () => {
    const s = createEmptyStore();
    createFolder(s, "F", () => "f1");
    addTag(s, tag("a"), "f1");
    const tags = collectTagsUnder(s, ["f1", "a"]);
    expect(tags.map(t => t.id)).toEqual(["a"]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/selection.test.ts`
Expected: FAIL —「Cannot find module .../selection」。

- [ ] **Step 3: 实现**

新建 `src/lodestar/selection.ts`:

```ts
import { findNode } from "./tree";
import { LodestarStore, TagNode, TreeNode } from "./types";

// Remove ids whose ancestor is also in the selection — deleting/operating on a
// folder already covers its descendants, so a mixed multi-select doesn't
// double-handle nested nodes.
export function pruneCovered(store: LodestarStore, ids: string[]): string[] {
  const set = new Set(ids);
  return ids.filter(id => {
    let cur = findNode(store, id);
    while (cur && cur.parent) {
      if (set.has(cur.parent.id)) return false;
      cur = findNode(store, cur.parent.id);
    }
    return true;
  });
}

// Expand a selection (tags + folders) into the flat list of tag nodes under it,
// in tree order, deduped. Folders contribute all of their nested tags.
export function collectTagsUnder(
  store: LodestarStore,
  ids: string[]
): TagNode[] {
  const out: TagNode[] = [];
  const seen = new Set<string>();
  function walk(node: TreeNode): void {
    if (node.type === "tag") {
      if (!seen.has(node.id)) {
        seen.add(node.id);
        out.push(node);
      }
    } else {
      for (const child of node.children) walk(child);
    }
  }
  for (const id of ids) {
    const found = findNode(store, id);
    if (found) walk(found.node);
  }
  return out;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx vitest run test/lodestar/selection.test.ts`
Expected: PASS（4 个用例）。

- [ ] **Step 5: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/lodestar/selection.ts test/lodestar/selection.test.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): pure selection helpers pruneCovered + collectTagsUnder

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: ② 批量接线(`deleteTag` / `deleteFolder` / `copyFolderLinks` 消费多选)

**Files:**
- Modify: `src/recorder/commands.ts:774-802`(`deleteTag` 的 Lodestar 分支吃 `additionalNodes`)
- Modify: `src/lodestar/commands.ts:185-201`(`copyFolderLinks` 吃多选)
- Modify: `src/lodestar/commands.ts:266-296`(`deleteFolder` 吃多选)
- Test: 无新单测;靠 Task 7 纯测试 + 构建 + 手测。

**Interfaces:**
- Consumes: `pruneCovered`, `collectTagsUnder`(selection.ts);`removeToTrash`, `findNode`(tree.ts);`confirmDelete`, `tagLinkMarkdown`(commands.ts 既有)。
- Produces: 多选时删除/复制对整组生效;单选时(数组长度 1)行为不变。

辅助:从一组树节点(tags+folders)提取 store id 的内联工具(复用 `handleDrag` 的提取逻辑):

```ts
// node 可能是 CodeTourStepNode(tag)或 CodeTourNode(folder)
function idOfTreeNode(n: any): string | undefined {
  if (n?.step?.id) return n.step.id;             // tag
  const tourId: string | undefined = n?.tour?.id; // folder
  return tourId ? tourId.split("::").pop() : undefined;
}
```

- [ ] **Step 1: `deleteTag` Lodestar 分支吃多选**

`src/recorder/commands.ts` 第 786-802 行的 `if ((node as any)?.step?.id) { ... }` 分支整体替换为(用选中集合而非单个 step):

```ts
      if ((node as any)?.step?.id) {
        const { getStore, saveStore } = await import("../lodestar/persistence");
        const { removeToTrash, findNode } = await import("../lodestar/tree");
        const { pruneCovered } = await import("../lodestar/selection");
        const { confirmDelete } = await import("../lodestar/commands");
        const { closeTagEditor } = await import("../lodestar/editThread");
        const store = getStore();

        const idOf = (n: any): string | undefined =>
          n?.step?.id ?? (n?.tour?.id ? n.tour.id.split("::").pop() : undefined);

        const selected =
          additionalNodes && additionalNodes.length > 0 ? additionalNodes : [node];
        const ids = pruneCovered(
          store,
          selected.map(idOf).filter((x): x is string => !!x)
        );
        if (ids.length === 0) return;

        let tagCount = 0;
        let folderCount = 0;
        for (const id of ids) {
          const f = findNode(store, id);
          if (f?.node.type === "folder") folderCount++;
          else tagCount++;
        }
        const parts: string[] = [];
        if (tagCount) parts.push(`${tagCount} 个标签`);
        if (folderCount) parts.push(`${folderCount} 个文件夹(及其中的标签)`);
        const summary =
          ids.length === 1 && tagCount === 1
            ? `删除标签「${((node as CodeTourStepNode).step!.description || "").split(/\r?\n/)[0].trim() || "(空注释)"}」?`
            : `删除${parts.join("、")}?`;
        if (!(await confirmDelete(summary))) return;

        for (const id of ids) {
          removeToTrash(store, id);
          closeTagEditor(id);
        }
        await saveStore();
        return;
      }
```

（`additionalNodes` 已是该命令第二参数,见第 778 行签名;`CodeTourStepNode` 已在 recorder/commands.ts 顶部 import。）

- [ ] **Step 2: `copyFolderLinks` 吃多选**

`src/lodestar/commands.ts` 的 `copyFolderLinks`(第 185 行）改签名并改用 `collectTagsUnder`:

```ts
export async function copyFolderLinks(node: any, additionalNodes?: any[]) {
  const store = getStore();
  const idOf = (n: any): string | undefined =>
    n?.tagLink?.id ?? n?.tagId ?? n?.step?.id ??
    (n?.tour?.id ? n.tour.id.split("::").pop() : undefined);
  const selected =
    additionalNodes && additionalNodes.length > 0 ? additionalNodes : [node];
  const ids = selected.map(idOf).filter((x): x is string => !!x);

  const { collectTagsUnder } = await import("./selection");
  const tags = collectTagsUnder(store, ids);
  if (tags.length === 0) {
    window.showInformationMessage("Code Jump Tags: 没有可复制的标签");
    return;
  }
  const links = tags.map(t =>
    tagLinkMarkdown({ note: t.note, file: t.file, line: t.line, pattern: t.pattern })
  );
  await env.clipboard.writeText(links.join("\n"));
  window.setStatusBarMessage(`Code Jump Tags: 已复制 ${links.length} 条链接`, 2000);
}
```

- [ ] **Step 3: `deleteFolder` 吃多选(委托给 deleteTag 的同款逻辑)**

为避免重复,`deleteFolder` 直接复用「批量删除」路径:把 `src/lodestar/commands.ts` 的 `deleteFolder` 改为(第 266 行起,保留 confirm/计数风格,与 Step 1 对齐):

```ts
export async function deleteFolder(node: any, additionalNodes?: any[]) {
  const store = getStore();
  const { pruneCovered } = await import("./selection");
  const idOf = (n: any): string | undefined =>
    n?.tour?.id ? n.tour.id.split("::").pop() : n?.step?.id;
  const selected =
    additionalNodes && additionalNodes.length > 0 ? additionalNodes : [node];
  const ids = pruneCovered(
    store,
    selected.map(idOf).filter((x): x is string => !!x)
  );
  if (ids.length === 0) {
    window.showInformationMessage("Code Jump Tags: 找不到该文件夹");
    return;
  }

  let tagCount = 0;
  let folderCount = 0;
  for (const id of ids) {
    const f = findNode(store, id);
    if (f?.node.type === "folder") folderCount++;
    else tagCount++;
  }
  const parts: string[] = [];
  if (folderCount) parts.push(`${folderCount} 个文件夹(及其中的标签)`);
  if (tagCount) parts.push(`${tagCount} 个标签`);
  const ok = await confirmDelete(`删除${parts.join("、")}?`);
  if (!ok) return;

  for (const id of ids) removeToTrash(store, id);
  await saveStore();
  closeTagEditor();
}
```

- [ ] **Step 4: 构建 + 回归**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npm run build && npx vitest run`
Expected: 零错误;vitest 全绿。

- [ ] **Step 5: 手测清单(GUI,需用户在 reload 后跑;此步记录预期)**

预期:① 侧栏多选若干标签 → 右键删除 → 一个汇总确认框 → 全部进回收站。② 多选标签 → 右键复制链接 → 剪贴板得到多行。③ 多选拖动 → 整组移动。④ 选中文件夹+其内标签一起删 → 只按文件夹计一次、不报错。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add src/recorder/commands.ts src/lodestar/commands.ts && git commit -m "$(cat <<'EOF'
feat(code-jump-tags): batch delete/copy consume multi-selection

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 文档 + CHANGELOG + 版本号

**Files:**
- Modify: `docs/code-jump-tags/功能-代码对照.md`(新增「未分组真文件夹 + 多选批量」章节)
- Modify: `CHANGELOG.md`(新增 `## 0.4.0` 条目)
- Modify: `package.json`(`version` `0.3.6` → `0.4.0`)

**Interfaces:** 无代码接口。

- [ ] **Step 1: 功能-代码对照**

在 `docs/code-jump-tags/功能-代码对照.md` 适当位置新增一段,说明:`FolderNode.inbox` 标记;`getOrCreateInbox`/`migrateLooseTags`/`renameFolderNode`/`moveNode` 毕业规则(`src/lodestar/tree.ts`);adapter 移除 `__loose__`(`src/lodestar/adapter.ts`);新标签落收件箱(`src/recorder/commands.ts`)与加载迁移(`src/lodestar/persistence.ts`);拖放接线与解除三处拦截(`src/player/tree/index.ts`、`src/lodestar/commands.ts`);② 批量纯函数 `pruneCovered`/`collectTagsUnder`(`src/lodestar/selection.ts`)被 `deleteTag`/`deleteFolder`/`copyFolderLinks` 消费多选;测试位置 `test/lodestar/inbox.test.ts`、`selection.test.ts`、`adapter.test.ts`。

- [ ] **Step 2: CHANGELOG**

`CHANGELOG.md` 顶部插入:

```markdown
## 0.4.0 - 2026-06-22

- 「未分组」现在是一个真正的文件夹:可以重命名、拖进别的文件夹、删除、在其下新建子文件夹。
  它仍然是新标签的落脚点(收件箱);一旦你给它改名、把它拖出根级或删除,它就变成普通文件夹,
  下次新建标签时根级会自动出现一个新的空「未分组」接着收。打开旧数据时,原先散落的标签会被
  自动归入「未分组」文件夹。
- 侧边栏支持多选批量操作:多选标签/文件夹后可一次性拖动、复制链接、删除(删除走一个汇总确认框)。
```

- [ ] **Step 3: 版本号**

`package.json` 的 `"version": "0.3.6"` 改为 `"0.4.0"`。

- [ ] **Step 4: 提交**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git add docs/code-jump-tags/功能-代码对照.md CHANGELOG.md package.json && git commit -m "$(cat <<'EOF'
docs(code-jump-tags): 0.4.0 changelog + feature/code map + version bump

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: 构建发版(打包 / 装本机 / 发市场)

**Files:** 无源码改动(发布流程)。参照 `docs/code-jump-tags/发布流程.md`。

**Interfaces:** 无。

- [ ] **Step 1: 最终全量验证**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npm run build && npx vitest run`
Expected: 零错误;全部测试绿(44 既有 + 新增 inbox 9 + selection 4 + adapter 2 = 约 59)。

- [ ] **Step 2: 打包 vsix**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && npx @vscode/vsce package --no-dependencies`
Expected: 生成 `code-jump-tags-0.4.0.vsix`。

- [ ] **Step 3: 装本机**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && "C:/Users/dell/AppData/Local/Programs/Microsoft VS Code/bin/code.cmd" --install-extension "$(pwd)/code-jump-tags-0.4.0.vsix" --force`
然后 `"C:/Users/dell/AppData/Local/Programs/Microsoft VS Code/bin/code.cmd" --list-extensions --show-versions | grep code-jump-tags`
Expected: `patrick1099.code-jump-tags@0.4.0`。

- [ ] **Step 4: 打 tag 推送触发市场发布**

```bash
cd /c/Users/dell/Desktop/plugin-research/codetour && git tag v0.4.0 && git push origin main && git push origin v0.4.0
```
Expected: `[new tag] v0.4.0 -> v0.4.0`;CI workflow `publish.yml` 触发。

- [ ] **Step 5: 确认 CI 发布成功**

Run: `cd /c/Users/dell/Desktop/plugin-research/codetour && "C:/Users/dell/AppData/Local/Programs/GitHub CLI/bin/gh.exe" run list --workflow=publish.yml --limit 1`
等待并确认状态 `success`。

- [ ] **Step 6: 提醒用户 reload**

告知用户:0.4.0 已装本机并发市场;唯一手动动作是 `Developer: Reload Window`,然后用 Task 8 Step 5 的手测清单在 GUI 验一遍。

---

## Self-Review

- **Spec 覆盖**:① inbox 字段/getOrCreateInbox(T1)、迁移(T2)、毕业(T3)、adapter 去 loose(T4)、新标签落收件箱+加载迁移(T5)、解除拦截+拖放(T6);② 纯函数(T7)、批量接线(T8);交付 文档/CHANGELOG/版本(T9)、发版(T10)。spec 各节均有对应任务。
- **占位符**:无 TBD/TODO;每个改动步骤含完整代码。
- **类型一致**:`getOrCreateInbox(store, idGen)`、`migrateLooseTags(store, idGen)`、`renameFolderNode(store, id, title)`、`pruneCovered(store, ids)`、`collectTagsUnder(store, ids)` 在定义任务与消费任务中签名一致;`INBOX_TITLE`/`newFolderId` 跨任务一致引用。
- **风险点**:T6/T8 为 VS Code 胶水,无单测,靠纯函数测试 + 构建 + 手测清单覆盖;已在步骤中显式列出手测预期。
